 I want to make an angular-electron app that we will build to an AppImage. This application must have controller support. We want to make an app similar to the plex HTPC app but for audiobookshelf. The user should be able to navigate the UI
  with only a game controller. I am running my own audiobookshelf server but I want this app to be shareable with other people so we cannot use API keys for authentication. For components we should not use material. FInd the best libraries we
  can use for components and we are focused on presenting a beutiful UI and UX experience while remaining minimalistic. The home page should be scrollable rows of "Continue Listening", "Next in Your Series", and "Suggestions" WE should have a
  left nav, the top option is the home page, second option is library, third is series, fourth is stats, at the bottom is settings. Library page should be a scrollable grid of the cover arts with filter and sorting options. All tiles should
  also have a progress indicator on books that have been started and a green circle with a check mark for ones that have been completed. We can also apply a dimming to the covers of the finished books. Series should show a fanned out stacking
  of all the book cover arts in the same series, selecting one opens a view just like the library screen but with just the books in the series. In the series page we will also display each tile of series in a scrollable list, but only one tile
   stacked vertically so the series art has room to fit most of the covers, they should still overlap like spreading cards on a table. Settings for now can just have a log out option. Stats should use beautiful data viz graphs to show all the
  user listening stats we can get from the audiobookshelf api. Remember to use component libraries, we want to maintain as little of our own component code as possible and should not reinvent low level components.